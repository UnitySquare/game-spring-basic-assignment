services:
  mysql:
    image: mysql:8.4
    container_name: crimson-citadel-mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: crimson_citadel
    ports:
      - "3306:3306"